training:
  wandb_base_run_name: "ptl"

model_cfg:
  model:
    params:
      losses:
        - type: PrototypicalGlobalLocalTripletLoss
          params:
            margin_global: 1.5
            margin_local: 0.5
            magnitude: 10
            num_classes: 13
            ignore_index: 254